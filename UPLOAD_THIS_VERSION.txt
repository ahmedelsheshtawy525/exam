FMVA Exam Platform v17

This version continues from v16.
Fixes:
- Adds the public /verify/:certificateNumber route so issued certificates open correctly.
- Keeps certificate verification public without login.
- Fixes admin certificate detail/revoke/restore routes to support TEXT certificate IDs.

Deploy this version after the v16 database migration is already applied.
